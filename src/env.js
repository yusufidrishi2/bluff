(function (window) {
    window.__env = window.__env || {};
  
    // API url
    window.__env = {
        LOCAL_TASKING_SYSTEM: "WEB_BROWSER",
        UPSTREAM_TASKING_SYSTEM: "STANDALONE"
    };  
}(this));
  
